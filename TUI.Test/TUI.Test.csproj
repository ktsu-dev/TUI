<Project>
  <Sdk Name="MSTest.Sdk" />
  <Sdk Name="ktsu.Sdk" />

  <PropertyGroup>
    <IsTestProject>true</IsTestProject>
    <TargetFramework>net10.0</TargetFramework>
    <TargetFrameworks></TargetFrameworks>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\TUI.Core\TUI.Core.csproj" />
    <!-- TUI.App already grants InternalsVisibleTo ktsu.TUI.Test; this is the reference that
         makes the demos it exposes actually reachable from a test. -->
    <ProjectReference Include="..\TUI.App\TUI.App.csproj" />
    <PackageReference Include="Moq" />
  </ItemGroup>

</Project>
